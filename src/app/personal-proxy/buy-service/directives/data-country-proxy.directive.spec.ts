import { DataCountryProxyDirective } from './data-country-proxy.directive';

describe('DataCountryProxyDirective', () => {
  it('should create an instance', () => {
    const directive = new DataCountryProxyDirective();
    expect(directive).toBeTruthy();
  });
});
