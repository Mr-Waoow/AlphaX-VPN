import { SubstringBetweenLetters } from './substring-between-letters';

describe('SubstringBetweenLetters', () => {
  it('should create an instance', () => {
    expect(new SubstringBetweenLetters()).toBeTruthy();
  });
});
