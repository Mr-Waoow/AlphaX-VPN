import { GetPropertyAndAssignPipe } from './get-property-and-assign.pipe';

describe('GetPropertyAndAssignPipe', () => {
  it('create an instance', () => {
    const pipe = new GetPropertyAndAssignPipe();
    expect(pipe).toBeTruthy();
  });
});
